
  # Organize project files

  This is a code bundle for Organize project files. The original project is available at https://www.figma.com/design/wDs4Nx9e5U6bwvP1lfXuLw/Organize-project-files.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  